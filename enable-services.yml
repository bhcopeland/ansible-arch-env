---
- name: enable default services
  service:
    name: "{{item}}"
    state: started
    enabled: yes
  sudo: yes
  with_items:
    - ntpd
    - dhcpcd
    - cups-browsed
    - avahi-daemon
    - lightdm
    - fancontrol